const express=require('express');
const router = express.Router();
const distanceController=require('../controller/mapBoxDistance')

router.get('/', )
router.put('/', )
router.post('/disatnce', distanceController);
router.delete('/', )


module.exports = router;
